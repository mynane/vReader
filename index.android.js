import React from 'react';
import {
  AppRegistry,
  Text,
} from 'react-native';

import ReactNativeVReaderApp from './source';

AppRegistry.registerComponent('vReader', () => ReactNativeVReaderApp);